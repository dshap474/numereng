"""Tracked marker for local agentic research programs."""

# Custom Programs

Put local, experiment-specific `*.md` programs in this directory and select one with
`metadata.agentic_research_program` in an experiment manifest.

All custom programs are gitignored. See
`src/numereng/features/agentic_research/AGENTS.md` for the full template and rules.
